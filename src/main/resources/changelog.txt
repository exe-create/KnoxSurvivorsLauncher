[0.3.2] Nested Linux game payload
- Accept Linux Steam installs where projectzomboid.sh stays in .../ProjectZomboid/ while projectzomboid.jar lives in .../ProjectZomboid/projectzomboid/. Launching still uses the parent wrapper script.

[Unreleased] Windows settings path
- Prefer the native ProjectZomboid64.exe so ProjectZomboid64.json supplies memory and other JVM settings.
- Put the game's bundled Java folders first on PATH for that process only to avoid mixed Java DLLs.
- Keep the bundled-Java BAT as a fallback when the native launcher or JSON file is unavailable.
- Check for launcher releases at startup and install a verified update automatically.
- Make the update button retry, download, and restart in one click when startup update checking fails.
- Stamp release JARs with their tag version so future automatic updates recognize the installed version.

[0.3.1] Runtime isolation fix
- Updated launcher validation for the current ZombieBuddy-compatible Knox Workshop runtime.
- Prevented Knox and ZombieBuddy patch systems from loading together in one game process.
- Fixed Windows release checksum generation and refreshed launcher verification coverage.

[0.3.0] First official launcher release
- Promoted the rc3 reliability/diagnostic work to the first stable launcher release.
- Windows uses Project Zomboid's bundled-Java BAT route for reliable Knox agent startup.
- System checks report verified/warning/failure states immediately and write detailed failure reasons to launcher.log.
- Workshop install/runtime validation, update checks, custom launch options and cross-platform packages remain included.

[0.3.0-rc3] Launcher reliability + diagnostics
- Windows launch keeps the working ProjectZomboid64.bat / bundled-Java route.
- System-check dots now use green only for verified checks, amber for checking/warnings, and red only for real failures.
- Checks update as they finish instead of waiting for the whole scan.
- Version/checksum/memory failures now explain what is wrong and write deeper diagnostics to launcher.log.
- The current Java NPC build is launcher-started; direct Steam startup is not advertised as equivalent.

[0.3.0-rc1] Release candidate
- Updated for the stable Workshop runtime filename: knox-agent.jar.
- A direct Steam startup path was tested during this release cycle; the launcher remains the supported Java-runtime startup method.
- Clarified which normal Project Zomboid settings carry over automatically and which launcher-only options must be copied into Steam.
- Refreshed public release documentation and version metadata.

[0.2.3-preview.3] Launcher refresh
- Keyed-art PLAY / INSTALL-UPDATE / SETTINGS buttons over the art.
- 16:9 background with cover-fit scaling and readability dim.
- Settings lives in its own dialog (debug, launch options, JVM memory).

[Earlier] IsoPlayer rebuild
- Workshop files + Java runtime checksum verified before launch.
- ZombieBuddy and Knox Launcher runtime paths are isolated to prevent double patching.
